# ⚠️ SUPERSEDED (2026-08-12, same day): this is the v1.0.0 measured run, preserved
# as the generating record of that version. Current record:
# MCL_CAPACITY_REALIZATION_20260812_v1.1.0.txt (source now at v1.1.0).
# The Part-5 conclusion below REMAINS VALID: its duplicate check was fail-open
# (4096-entry witness cap), but a tuple duplicate forces a 64-bit digest hit and
# this run observed hash_hits = 0, which proves 0 duplicates regardless.
# Parts 2/4 values differ in v1.1.0 by design (rows decorrelated; 8 tuples).
#
# MCL Capacity Realization Experiment — measured run
# host: arm64 Darwin | compiler: Apple clang version 16.0.0 (clang-1600.0.26.6)
# date: 2026-08-12 | build: c++ -std=c++17 -O3 -Wall -Wextra -Wconversion -I ..

================================================================
 MCL CAPACITY REALIZATION EXPERIMENT   MCL-CAPACITY-REALIZATION-2026-0812-001
 version 1.0.0 | engine 8.1.0
 nominal key length is not realized security; capacity is
================================================================

[1] NOMINAL CAPACITY ACCOUNTING (analytic, [0019]-[0020], [0044])
    key length L                              = 256 bits
    config A: 2 variables /  2 weights [2,2^30) =  60.00 bits capacity
    config B: 4 variables / 12 weights [2,2^30) = 360.00 bits capacity
    N(N-1)*b rule: A -> 2*30 = 60, B -> 12*30 = 360
  [PASS] config A capacity < L  => a 256-bit key CANNOT be represented (the deficiency)
  [PASS] config B capacity >= L => the capacity relation of [0019] is satisfied

[2] COUNTING-MODEL VALIDATION (measured first-collision vs analytic)
    c(bits)   trials   measured mean   analytic sqrt(pi/2)*2^(c/2)   ratio
       16        8           375.9                        318.3   1.181
       20        8          1313.8                       1280.9   1.026
       24        8          5502.6                       5131.1   1.072
       28        8         22406.0                      20531.8   1.091
  [PASS] measured first-collision counts match the 2^C counting model (within 2x)
    => the model is validated on the real KDF; it is the same model that
       gives 2^60 for config A and 2^360 for config B.

[3] EQUIVALENT-KEY DEMONSTRATION (capacity-bounded security, config A)
    reduced config: 2 weights in [2,2^12)  => capacity 24.00 bits
    found after 9443 derivations:
      key #1150 -> (p,q) = (2982, 3857)
      key #9443 -> (p,q) = (2982, 3857)
  [PASS] the two 256-bit keys are DISTINCT
  [PASS] yet they derive the SAME parameter tuple
    keystream(key #1150) vs keystream(key #9443): Hamming = 0.00%  (BYTE-IDENTICAL)
    control, distinct tuple (p,q) = (806, 4028):     Hamming = 50.00%
  [PASS] distinct keys, same tuple => IDENTICAL output (keys are equivalent)
  [PASS] control: a distinct tuple gives ~50% Hamming (outputs independent)
    => realized security is bounded by the PARAMETER space (24 bits),
       not by the 256-bit key that was supplied.

[4] STATISTICAL CONTROL — the deficient configuration still looks good
    (p,q) = (1984, 1142), 1 MiB keystream
    Shannon entropy = 7.999819 bits/byte   (ideal 8.000000)
    chi-square      = 262.88  (df=255, 99% threshold 310.46)
  [PASS] capacity-deficient config passes the entropy check
  [PASS] capacity-deficient config passes the chi-square check
    => statistical quality and a fully-chaotic range do NOT reveal the
       capacity deficiency. Requirement-level rules cannot detect it;
       only the sizing relation of [0019]-[0020] does.

[5] REALIZED CAPACITY OF THE FULL CONFIGURATION (4 vars / 12 weights)
    sampled 1048576 keys through the production derivation
    64-bit digest hits = 0 (expected ~3.0e-08 by birthday on the DIGEST,
      not on the tuple), verified true tuple duplicates = 0
  [PASS] no parameter-tuple collision over the sampled budget
    single-bit key avalanche over 48 trials: mean 49.91% (min 48.07%, max 51.44%)
  [PASS] one flipped key bit => ~50% output change (capacity realized at output)
    unrelated-key pairs over 24 pairs: mean Hamming 50.01%
  [PASS] distinct keys => independent output (no hidden collapse of the tuple space)

[6] SUMMARY — nominal vs realized, and the extrapolation
    +----------------------+-----------+-----------+----------------------+
    | configuration        | key given | capacity  | realized key search  |
    +----------------------+-----------+-----------+----------------------+
    | A: 2 vars /  2 wts   | 256 bits  |   60.0 b  | 2^60 (capacity-bound)|
    | B: 4 vars / 12 wts   | 256 bits  |  360.0 b  | 2^256 (key-bound)    |
    +----------------------+-----------+-----------+----------------------+
    Both configurations: same key length, same derivation, same chaotic
    range, both statistically clean (Part 4). They differ by ~196 bits of
    realized security solely through the parameter representation.
    Expected first collision: A ~ 1.35e+09 keys (reachable);  B ~ 1.92e+54 keys (unreachable).
    Parts 2-3 measured the effect at reduced capacity and validated the
    model that produces these two numbers; the 2^-360 event is not observed.

================================================================
  SUMMARY: 12 passed, 0 failed
================================================================
